# Calor handoff — 16 juli 2026

Vervolg op [HANDOFF_2026-07-14.md](HANDOFF_2026-07-14.md). Alles daaruit blijft gelden tenzij hieronder anders staat.

## Commits van vandaag (Slice 2, `feat/studio-tiktok-autopost`)

```text
f77613f  feat(tripfit): Slice 2 baseline - Supabase magic-link/Google auth, sessie-proxy, private shells
4e36197  feat(tripfit): seed volgt country pack - pack-ids als DB-ids, 13 regio's, interests gelijkgetrokken
ab1d9d6  feat(tripfit): next-redirect door auth-flow met veilige padvalidatie
c951e87  feat(tripfit): buildTripRecord - previewinput naar transactionele trip-writes, pack-gevalideerd
a66fe88  feat(tripfit): preview naar private trip - transactionele save-action, CTA live, auth-redirect
fbd8a5a  feat(tripfit): dashboard-triplijst en trip-detail met server-side ownership
a2dc60e  feat(tripfit): RLS-migratie - default deny via API, owner-select op trips en kindrecords
```

## Wat werkt nu — Slice 2 compleet

- **Auth**: magic link + Google via Supabase (`/auth`), sessie-refresh in `proxy.ts`, `next`-redirect door de hele flow met veilige padvalidatie (`sanitizeNextPath` — `//evil` en `/\evil` vallen terug op `/dashboard`).
- **Preview → private trip**: de CTA "Maak account & bewaar" is live. Uitgelogd → `/auth?next=<preview>`; ingelogd → transactionele write (`Trip` + `TripStop` + `Traveler` + `TripInterest` in één `$transaction`) → redirect `/trips/[id]`. Zonder Supabase-env blijft de knop netjes disabled (credential-vrij blijft werken).
- **Seed = country pack**: pack-ids zijn DB-primary-keys (13 regio's, `do-*`), interests volgen `InterestId` exact. Previewinput is daardoor direct FK-geldig.
- **Dashboard + trip-detail**: `/dashboard` toont de trips van de ingelogde gebruiker; `/trips/[id]` dwingt ownership af ín de query (`userId` in de `where`) en reconstrueert de living preview deterministisch (round-trip unit-getest).
- **RLS**: migratie `20260716120000_enable_rls` — alle 21 app-tabellen dicht via PostgREST, catalogus read-only, owner-select op trips + kindrecords. Prisma is table owner en wordt niet geraakt.
- **Kanttekening**: live magic-link-e2e (echte mailbox) vereist een echt Supabase-project; ownership is via unit tests afgedekt, route-bescherming via e2e.

## Verificatie (16 juli)

```text
npm run typecheck   ✅
npm run lint        ✅
npm run test        ✅ 11 files / 44 tests
npm run db:validate ✅
npm run build       ✅
npm run test:e2e    ✅ 10 tests (Pixel 7 + desktop Chromium)
```

## ⚠️ Nieuw: deploy-infra bestaat al (deels)

Rowan heeft op 16 juli aangemaakt:

- **GitHub-repo**: `TiveauHRTMN/calortravel` (public, alleen een README — wacht op de code)
- **Vercel-project**: `vercel.com/tiveauhrtmns-projects/calortravel` (was op 16 juli nog niet zichtbaar via de API — mogelijk net aangemaakt of nog niet af)

Dit vervangt de eerdere aanpak "Vercel-project met Root Directory `tripfit` op de kutweer-repo". De code verhuist naar een eigen repo.

## Plan voor de volgende sessie (volgorde)

1. **Code naar `TiveauHRTMN/calortravel` pushen.** Aanpak: `git subtree split --prefix=tripfit -b calor-export` vanuit kutweer (behoudt de tripfit-historie) en die branch naar `main` van de nieuwe repo pushen (README daar mag weg of overschreven). Alternatief: schone kopie zonder historie. Daarna is de vraag wáár doorontwikkeld wordt (nieuwe repo = schoonst; kutweer blijft dan read-only voor Calor).
2. **Vercel-project koppelen**: repo `calortravel` connecten (Root Directory = repo-root), domein calortravel.nl koppelen, DNS bij de registrar. **Git-based deployen, geen CLI-dirty-tree.**
3. **Supabase-project aanmaken** + env-vars in Vercel: `DATABASE_URL`/`DIRECT_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_APP_URL=https://calortravel.nl`. Dan `db:migrate:deploy` + `db:seed`, en de Google-provider in Supabase aanzetten (magic link werkt zonder extra provider-config).
4. **Slice 3 — de dagview** (DESIGN_DIRECTION.md §5.3 en §9 stap 2): dageditie met levend paneel, fit-trace, Plan B, "sinds gisteren". Referentie-mockup: artifact "Calor — Live dagview".
5. **Klein/open** (uit 14 juli): logo vectoriseren (SVG voor druk/borduurwerk) **[TE VALIDEREN]**; OG-image op het nieuwe merk.

## Handoff in één zin

Slice 2 is af en gecommit (auth, opslaan, dashboard, detail, RLS — alles groen); volgende sessie: code naar de nieuwe calortravel-repo, Vercel + DNS + Supabase-env, dan Slice 3.
